diff --git a/src/libstore/build/substitution-goal.cc b/src/libstore/build/substitution-goal.cc
index 9027349..7fb87ed 100644
--- a/src/libstore/build/substitution-goal.cc
+++ b/src/libstore/build/substitution-goal.cc
@@ -6,6 +6,9 @@
 #include "nix/store/worker-settings.hh"
 #include "nix/util/signals.hh"
 #include "nix/util/callback.hh"
+#include "nix/store/binary-cache-store.hh"
+#include "nix/store/local-store.hh"
+#include "nix/store/styx.hh"
 
 namespace nix {
 
@@ -204,7 +207,7 @@ Goal::Co PathSubstitutionGoal::tryToRun(
     auto maintainRunningSubstitutions = std::make_unique<MaintainCount<uint64_t>>(worker.runningSubstitutions);
     worker.updateProgress();
 
-    auto promise = std::promise<void>();
+    auto promise = std::promise<StyxMode>();
     auto future = promise.get_future();
 
     /* Be careful with ownership. cleanup() doesn't signal the worker thread
@@ -217,6 +220,7 @@ Goal::Co PathSubstitutionGoal::tryToRun(
                        storePath = storePath,
                        repair = repair,
                        sub,
+                       info,
                        maybeWaker = worker.getCrossThreadWaker(),
                        maybeWorkerStore = worker.store.weak_from_this()]() mutable {
         try {
@@ -227,15 +231,43 @@ Goal::Co PathSubstitutionGoal::tryToRun(
             if (!workerStore)
                 return;
 
+            /* Styx can provide paths from a binary cache to a local store.
+               It can't repair a path, so repairs use regular substitution. */
+            auto cacheSrc = sub.dynamic_pointer_cast<BinaryCacheStore>();
+            auto localDst = std::dynamic_pointer_cast<LocalStore>(workerStore);
+            auto sourceUri = sub->config.getReference().render(/*withParams=*/false);
+            auto styxMode = (cacheSrc && localDst && repair == NoRepair)
+                                ? canUseStyx(sourceUri, info->narSize, std::string(info->path.name()))
+                                : StyxDisable;
+            auto checkSigs = sub->config.isTrusted ? NoCheckSigs : CheckSigs;
+
             Activity act(
                 *logger,
                 actSubstitute,
-                Logger::Fields{workerStore->printStorePath(storePath), sub->config.getHumanReadableURI()});
+                Logger::Fields{
+                    workerStore->printStorePath(storePath),
+                    std::string(styxMode == StyxDisable ? "" : "STYX:") + sub->config.getHumanReadableURI()});
             PushActivity pact(act.id);
 
-            copyStorePath(*sub, *workerStore, subPath, repair, sub->config.isTrusted ? NoCheckSigs : CheckSigs);
+            if (styxMode != StyxDisable) {
+                try {
+                    if (styxMode == StyxMount)
+                        localDst->mountStyx(sourceUri, *info, checkSigs);
+                    else
+                        localDst->materializeStyx(sourceUri, *info, checkSigs);
+                } catch (std::exception & e) {
+                    printError(
+                        "styx failed for '%s', falling back to substitution: %s",
+                        workerStore->printStorePath(storePath),
+                        e.what());
+                    styxMode = StyxDisable; // fall through to regular substitution
+                }
+            }
+
+            if (styxMode == StyxDisable)
+                copyStorePath(*sub, *workerStore, subPath, repair, checkSigs);
 
-            promise.set_value();
+            promise.set_value(styxMode);
         } catch (...) {
             promise.set_exception(std::current_exception());
         }
@@ -258,8 +290,9 @@ Goal::Co PathSubstitutionGoal::tryToRun(
     thr.join();
     worker.childTerminated(this);
 
+    StyxMode styxMode = StyxDisable;
     try {
-        future.get();
+        styxMode = future.get();
     } catch (std::exception & e) {
         /* Cause the parent build to fail unless --fallback is given,
            or the substitute has disappeared. The latter case behaves
@@ -281,7 +314,17 @@ Goal::Co PathSubstitutionGoal::tryToRun(
 
     worker.markContentsGood(storePath);
 
-    printMsg(lvlChatty, "substitution of path '%s' succeeded", worker.store.printStorePath(storePath));
+    switch (styxMode) {
+    case StyxDisable:
+        printMsg(lvlChatty, "substitution of path '%s' succeeded", worker.store.printStorePath(storePath));
+        break;
+    case StyxMount:
+        printMsg(lvlInfo, "mounted '%s' with styx", worker.store.printStorePath(storePath));
+        break;
+    case StyxMaterialize:
+        printMsg(lvlInfo, "materialized '%s' with styx", worker.store.printStorePath(storePath));
+        break;
+    }
 
     maintainRunningSubstitutions.reset();
 
diff --git a/src/libstore/gc.cc b/src/libstore/gc.cc
index 8e997c8..9cff3bb 100644
--- a/src/libstore/gc.cc
+++ b/src/libstore/gc.cc
@@ -12,6 +12,7 @@
 #include "nix/util/util.hh"
 #include "nix/util/file-system.hh"
 #include "nix/store/posix-fs-canonicalise.hh"
+#include "nix/store/styx.hh"
 
 #include "store-config-private.hh"
 
@@ -557,6 +558,10 @@ void LocalStore::collectGarbage(const GCOptions & options, GCResults & results)
             }
         }
 
+        /* A styx mount is read-only, so it has to go first. */
+        if (!removeStyxMount(realPath.string(), std::string(baseName)))
+            return;
+
         printInfo("deleting '%1%'", path);
 
         results.paths.insert(path);
@@ -713,6 +718,9 @@ void LocalStore::collectGarbage(const GCOptions & options, GCResults & results)
             }
         }
         for (auto & path : topoSortPaths(visited)) {
+            /* Kept alive below, or referenced by a path that was. */
+            if (alive.contains(path))
+                continue;
             if (!dead.insert(path).second)
                 continue;
             if (shouldDelete) {
@@ -743,6 +751,15 @@ void LocalStore::collectGarbage(const GCOptions & options, GCResults & results)
                     wakeup.notify_all();
                 });
                 try {
+                    /* Remove a styx mount before invalidating the path, so
+                       that a mount that can't be removed leaves it valid. */
+                    if (!removeStyxMount(
+                            (config->realStoreDir.get() / std::string(path.to_string())).string(),
+                            std::string(path.to_string()))) {
+                        dead.erase(path);
+                        markAlive(path);
+                        continue;
+                    }
                     invalidatePathChecked(path);
                     deleteFromStore(path.to_string(), true);
                     referrersCache.erase(path);
diff --git a/src/libstore/include/nix/store/globals.hh b/src/libstore/include/nix/store/globals.hh
index 34eab7f..91d940a 100644
--- a/src/libstore/include/nix/store/globals.hh
+++ b/src/libstore/include/nix/store/globals.hh
@@ -421,6 +421,33 @@ public:
           Set it to 1 to warn on all paths.
         )"};
 
+    Setting<Strings> styxSubstituters{
+        this, Strings{"https://cache.nixos.org/"}, "styx-substituters", "Use Styx for these substituters."};
+
+    Setting<uint64_t> styxMinSize{
+        this, 64 * 1024, "styx-min-size", "Use Styx only for paths larger than this nar size."};
+
+    Setting<Strings> styxOndemand{
+        this,
+        Strings{},
+        "styx-ondemand",
+        "List of package name regexes to mount using Styx (empty means never use)"};
+
+    Setting<Strings> styxMaterialize{
+        this,
+        Strings{},
+        "styx-materialize",
+        "List of package name regexes to materialize using Styx (empty means never use)"};
+
+    Setting<Strings> styxExclude{
+        this,
+        Strings{},
+        "styx-exclude",
+        "List of package name regexes to deny using Styx (overrides styx-ondemand/materialize)"};
+
+    Setting<std::string> styxSockPath{
+        this, "/var/cache/styx/styx.sock", "styx-sock-path", "Path to Styx socket."};
+
     /**
      * Get the options needed for profile directory functions.
      */
diff --git a/src/libstore/include/nix/store/local-store.hh b/src/libstore/include/nix/store/local-store.hh
index 0851795..ae1c330 100644
--- a/src/libstore/include/nix/store/local-store.hh
+++ b/src/libstore/include/nix/store/local-store.hh
@@ -306,6 +306,9 @@ public:
 
     void addTempRoot(const StorePath & path) override;
 
+    void mountStyx(const std::string & sourceUri, const ValidPathInfo & info, CheckSigsFlag checkSigs);
+    void materializeStyx(const std::string & sourceUri, const ValidPathInfo & info, CheckSigsFlag checkSigs);
+
 private:
 
     void createTempRootsFile();
diff --git a/src/libstore/include/nix/store/meson.build b/src/libstore/include/nix/store/meson.build
index 3ccf0ad..7be7bb9 100644
--- a/src/libstore/include/nix/store/meson.build
+++ b/src/libstore/include/nix/store/meson.build
@@ -93,6 +93,7 @@ headers = [ config_pub_h ] + files(
   'store-open.hh',
   'store-reference.hh',
   'store-registration.hh',
+  'styx.hh',
   'uds-remote-store.hh',
   'worker-protocol-connection.hh',
   'worker-protocol-impl.hh',
diff --git a/src/libstore/include/nix/store/styx.hh b/src/libstore/include/nix/store/styx.hh
new file mode 100644
index 0000000..1362f45
--- /dev/null
+++ b/src/libstore/include/nix/store/styx.hh
@@ -0,0 +1,28 @@
+#pragma once
+///@file
+
+#include <cstdint>
+#include <string>
+
+namespace nix {
+
+typedef enum {
+    StyxDisable,
+    StyxMount,
+    StyxMaterialize
+} StyxMode;
+
+StyxMode canUseStyx(const std::string storeUri, uint64_t narSize, std::string name);
+void makeStyxMount(const std::string upstream, const std::string storePath, const std::string mountPoint, uint64_t narSize);
+void makeStyxMaterialize(const std::string upstream, const std::string storePath, const std::string dest, uint64_t narSize);
+bool isStyxMount(const std::string path);
+void deleteStyxMount(const std::string path);
+/**
+ * Make sure there is no styx mount at `realPath`, for GC. Returns false if
+ * one is still there, and then the path must not be deleted or
+ * invalidated. Never throws, so that a failure can't abort GC.
+ */
+bool removeStyxMount(const std::string & realPath, const std::string & storePath);
+void tryStyxGC();
+
+}  // namespace nix
diff --git a/src/libstore/local-store.cc b/src/libstore/local-store.cc
index 909f223..44ac6e3 100644
--- a/src/libstore/local-store.cc
+++ b/src/libstore/local-store.cc
@@ -17,6 +17,7 @@
 #include "nix/store/keys.hh"
 #include "nix/util/users.hh"
 #include "nix/store/store-registration.hh"
+#include "nix/store/styx.hh"
 
 #include <algorithm>
 #include <cstring>
@@ -1313,6 +1314,62 @@ StorePath LocalStore::addToStoreFromDump(
     return dstPath;
 }
 
+void LocalStore::mountStyx(const std::string & sourceUri, const ValidPathInfo & info, CheckSigsFlag checkSigs)
+{
+    if (checkSigs && pathInfoIsUntrusted(info))
+        throw Error("cannot add path '%s' because it lacks a signature by a trusted key", printStorePath(info.path));
+
+    addTempRoot(info.path);
+
+    if (!isValidPath(info.path)) {
+        PathLocks outputLock;
+
+        auto realPath = toRealPath(info.path);
+
+        /* Lock the output path.  But don't lock if we're being called
+           from a build hook (whose parent process already acquired a
+           lock on this path). */
+        if (!locksHeld.count(printStorePath(info.path)))
+            outputLock.lockPaths({realPath});
+
+        /* The path may have been created by another process in the meantime, so check again. */
+        if (!isValidPathUncached(info.path)) {
+            makeStyxMount(sourceUri, std::string(info.path.to_string()), realPath.string(), info.narSize);
+            registerValidPath(info);
+        }
+
+        outputLock.setDeletion(true);
+    }
+}
+
+void LocalStore::materializeStyx(const std::string & sourceUri, const ValidPathInfo & info, CheckSigsFlag checkSigs)
+{
+    if (checkSigs && pathInfoIsUntrusted(info))
+        throw Error("cannot add path '%s' because it lacks a signature by a trusted key", printStorePath(info.path));
+
+    addTempRoot(info.path);
+
+    if (!isValidPath(info.path)) {
+        PathLocks outputLock;
+
+        auto realPath = toRealPath(info.path);
+
+        /* Lock the output path.  But don't lock if we're being called
+           from a build hook (whose parent process already acquired a
+           lock on this path). */
+        if (!locksHeld.count(printStorePath(info.path)))
+            outputLock.lockPaths({realPath});
+
+        /* The path may have been created by another process in the meantime, so check again. */
+        if (!isValidPathUncached(info.path)) {
+            makeStyxMaterialize(sourceUri, std::string(info.path.to_string()), realPath.string(), info.narSize);
+            registerValidPath(info);
+        }
+
+        outputLock.setDeletion(true);
+    }
+}
+
 /* Create a temporary directory in the store that won't be
    garbage-collected until the returned FD is closed. */
 std::pair<std::filesystem::path, AutoCloseFD> LocalStore::createTempDirInStore()
diff --git a/src/libstore/meson.build b/src/libstore/meson.build
index c266f05..1456916 100644
--- a/src/libstore/meson.build
+++ b/src/libstore/meson.build
@@ -356,6 +356,7 @@ sources = files(
   'store-dir-config.cc',
   'store-reference.cc',
   'store-registration.cc',
+  'styx.cc',
   'uds-remote-store.cc',
   'worker-protocol-connection.cc',
   'worker-protocol.cc',
diff --git a/src/libstore/optimise-store.cc b/src/libstore/optimise-store.cc
index fa19b08..8c32232 100644
--- a/src/libstore/optimise-store.cc
+++ b/src/libstore/optimise-store.cc
@@ -4,6 +4,7 @@
 #include "nix/store/posix-fs-canonicalise.hh"
 #include "nix/util/source-accessor.hh"
 #include "nix/util/file-system.hh"
+#include "nix/store/styx.hh"
 
 #include <cstdlib>
 #include <cstring>
@@ -310,7 +311,11 @@ void LocalStore::optimiseStore(OptimiseStats & stats)
         addTempRoot(i);
         if (!isValidPath(i))
             continue; /* path was GC'ed, probably */
-        {
+        /* A styx mount is read-only and on another filesystem, and reading
+           it can fetch its whole contents. */
+        if (isStyxMount((config->realStoreDir.get() / i.to_string()).string()))
+            debug("skipping styx mount '%s'", printStorePath(i));
+        else {
             Activity act(*logger, lvlTalkative, actUnknown, fmt("optimising path '%s'", printStorePath(i)));
             optimisePath_(&act, stats, config->realStoreDir.get() / i.to_string(), inodeHash, NoRepair);
         }
diff --git a/src/libstore/styx.cc b/src/libstore/styx.cc
new file mode 100644
index 0000000..ffe8aac
--- /dev/null
+++ b/src/libstore/styx.cc
@@ -0,0 +1,220 @@
+
+#include "nix/util/error.hh"
+#include "nix/util/file-system.hh"
+#include "nix/store/globals.hh"
+#include "nix/util/logging.hh"
+#include "nix/store/styx.hh"
+
+#include <cerrno>
+#include <cstring>
+#include <regex>
+#include <map>
+#include <memory>
+#include <mutex>
+#include <sys/mount.h>
+#include <sys/stat.h>
+#include <sys/vfs.h>
+
+#include <nlohmann/json.hpp>
+#include <curl/curl.h>
+
+namespace nix {
+
+static size_t writeCallback(char *ptr, size_t size, size_t nmemb, void *userdata) {
+    ((std::string*)userdata)->append(ptr, size * nmemb);
+    return size * nmemb;
+}
+
+static void styxRequest(const std::string path, const nlohmann::json & req) {
+    auto postData = req.dump();
+
+    CURL *curl = curl_easy_init();
+    if (!curl) {
+        throw Error("curl init failed");
+    }
+
+    std::string url = "http://unix" + path;
+    curl_easy_setopt(curl, CURLOPT_URL, url.c_str());
+    curl_easy_setopt(curl, CURLOPT_UNIX_SOCKET_PATH, settings.styxSockPath.get().c_str());
+    curl_easy_setopt(curl, CURLOPT_POST, 1L);
+    curl_easy_setopt(curl, CURLOPT_POSTFIELDS, postData.c_str());
+    curl_easy_setopt(curl, CURLOPT_POSTFIELDSIZE, postData.size());
+    // TODO: this doesn't seem to work for unix sockets?
+    curl_easy_setopt(curl, CURLOPT_CONNECTTIMEOUT, 1);
+    curl_easy_setopt(curl, CURLOPT_TIMEOUT, 300);
+
+    std::string resData;
+    curl_easy_setopt(curl, CURLOPT_WRITEFUNCTION, writeCallback);
+    curl_easy_setopt(curl, CURLOPT_WRITEDATA, &resData);
+
+    CURLcode curlRes = curl_easy_perform(curl);
+    curl_easy_cleanup(curl);
+    if (curlRes != CURLE_OK) {
+        throw Error("styx error: curl: %d", curlRes);
+    }
+    nlohmann::json res = nlohmann::json::parse(resData);
+    if (!res.value("Success", false)) {
+        throw Error(res.value("Error", "<missing>"));
+    }
+}
+
+// Compiles each styx-* pattern once. An invalid pattern is reported once and
+// gives null, so a bad setting can't break substitution.
+static const std::regex * styxRegex(const std::string & pattern) {
+    static std::mutex lock;
+    static std::map<std::string, std::unique_ptr<std::regex>> cache;
+    std::lock_guard<std::mutex> guard(lock);
+    auto i = cache.find(pattern);
+    if (i == cache.end()) {
+        std::unique_ptr<std::regex> re;
+        try {
+            re = std::make_unique<std::regex>(pattern);
+        } catch (std::regex_error & e) {
+            warn("ignoring invalid styx pattern '%s': %s", pattern, e.what());
+        }
+        i = cache.emplace(pattern, std::move(re)).first;
+    }
+    return i->second.get();
+}
+
+StyxMode canUseStyx(const std::string storeUri, uint64_t narSize, std::string name) {
+    auto trim = [](std::string_view s) {
+        return s.back() == '/' ? s.substr(0, s.size() - 1) : s;
+    };
+    // always use styx on fake cache, even if nar is too small
+    // TODO: maybe easier to do this a different way so we don't need a
+    // special case here
+    if (trim(storeUri) != trim("http://localhost:7444")) {
+        bool useStyx = false;
+        for (auto & uri : settings.styxSubstituters.get())
+            if (trim(uri) == trim(storeUri)) { useStyx = true; break; }
+        if (!useStyx || narSize < settings.styxMinSize)
+            return StyxDisable;
+    }
+    for (auto & exc : settings.styxExclude.get()) {
+        auto re = styxRegex(exc);
+        // an exclusion we can't evaluate excludes everything
+        if (!re || std::regex_match(name, *re))
+            return StyxDisable;
+    }
+    for (auto & inc : settings.styxOndemand.get())
+        if (auto re = styxRegex(inc); re && std::regex_match(name, *re))
+            return StyxMount;
+    for (auto & inc : settings.styxMaterialize.get())
+        if (auto re = styxRegex(inc); re && std::regex_match(name, *re))
+            return StyxMaterialize;
+    return StyxDisable;
+}
+
+void makeStyxMount(const std::string upstream, const std::string storePath, const std::string mountPoint, uint64_t narSize)
+{
+    deletePath(mountPoint);
+    createDirs(mountPoint);
+
+    nlohmann::json req = {
+        {"Upstream", upstream},
+        {"StorePath", storePath},
+        {"MountPoint", mountPoint},
+        {"NarSize", narSize},
+    };
+    styxRequest("/mount", req);
+
+    // Check that the mount is really there before the caller registers
+    // the path as valid.
+    if (!isStyxMount(mountPoint))
+        throw Error("styx reported mounting '%s', but it is not mounted", mountPoint);
+}
+
+
+void makeStyxMaterialize(const std::string upstream, const std::string storePath, const std::string dest, uint64_t narSize)
+{
+    nlohmann::json req = {
+        {"Upstream", upstream},
+        {"StorePath", storePath},
+        {"DestPath", dest},
+        {"NarSize", narSize},
+    };
+    styxRequest("/materialize", req);
+}
+
+
+bool isStyxMount(const std::string mountPoint)
+{
+    // A valid path may be missing on disk (GC tolerates that), and statfs
+    // follows symlinks, so a symlink into a styx mount would look like one.
+    struct stat lst;
+    if (lstat(mountPoint.c_str(), &lst)) {
+        if (errno == ENOENT)
+            return false;
+        throw SysError("getting status of '%s'", mountPoint);
+    }
+    if (S_ISLNK(lst.st_mode))
+        return false;
+
+    struct statfs st;
+    if (statfs(mountPoint.c_str(), &st))
+        throw SysError("getting filesystem info about '%s'", mountPoint);
+    return st.f_type == 0xE0F5E1E2;
+}
+
+
+void deleteStyxMount(const std::string storePath)
+{
+    nlohmann::json req = {
+        {"StorePath", storePath},
+    };
+    styxRequest("/umount", req);
+    // note: this does not delete the mountpoint, but collectGarbage will do
+    // that right after calling this
+}
+
+static bool stillStyxMount(const std::string & realPath)
+{
+    try {
+        return isStyxMount(realPath);
+    } catch (std::exception & e) {
+        printError("cannot check whether '%s' is a styx mount: %s", realPath, e.what());
+        return true;
+    }
+}
+
+bool removeStyxMount(const std::string & realPath, const std::string & storePath)
+{
+    if (!stillStyxMount(realPath))
+        return true;
+    try {
+        deleteStyxMount(storePath);
+    } catch (std::exception & e) {
+        warn("styx could not unmount '%s': %s", realPath, e.what());
+    }
+    if (!stillStyxMount(realPath))
+        return true;
+    // The daemon is down or has no record of this mount, so detach it
+    // ourselves, as the daemon would have. When the daemon restores its
+    // mounts, it forgets the ones whose mount point is gone.
+    if (umount2(realPath.c_str(), MNT_DETACH) == -1) {
+        printError("not deleting '%s': cannot unmount it: %s", realPath, strerror(errno));
+        return false;
+    }
+    if (stillStyxMount(realPath)) {
+        printError("not deleting '%s': it is still a styx mount", realPath);
+        return false;
+    }
+    return true;
+}
+
+void tryStyxGC()
+{
+    try {
+        std::map<int, bool> byState;
+        byState[5] = true;
+        nlohmann::json req = {
+            {"GcByState", byState},
+        };
+        styxRequest("/gc", req);
+    } catch (...) {
+        // ignore
+    }
+}
+
+}  // namespace nix
